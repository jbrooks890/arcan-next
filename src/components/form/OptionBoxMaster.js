import SelectBox from "./SelectBox";
import ChoiceBox from "./ChoiceBox";

const OptionBoxMaster = ({
  options,
  display,
  field,
  fieldPath,
  required,
  single = true,
  label,
  className,
  value = [],
  handleChange,
}) => {
  return <div>OptionBoxMaster</div>;
};

export default OptionBoxMaster;
